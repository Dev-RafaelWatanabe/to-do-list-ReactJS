# Dockerfile para desenvolvimento (com hot-reload)
FROM node:18-alpine

WORKDIR /app

# Copiar package.json
COPY package*.json ./

# Instalar todas as dependências (incluindo dev)
RUN npm install

# Copiar código fonte
COPY . .

# Expor porta
EXPOSE 3000

# O comando será definido no docker-compose.dev.yml
CMD ["npm", "run", "start:dev"]
